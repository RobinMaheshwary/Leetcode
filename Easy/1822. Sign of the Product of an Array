class Solution {
public:
    int arraySign(vector<int>& nums) {
        int n=1;
        for(int i=0;i<nums.size();i++){
            if(nums[i]<0) n*=-1;
            else if(nums[i]>0) n*=1;
            else n=0;
        }
        
        return n;
    }

};
